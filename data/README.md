# data/

Generated files live here — not committed (see `.gitignore`), regenerated locally by the adapter scripts in `scripts/`.

Run `python3 scripts/fetch_la_sweeping.py` to produce `data/la-sweeping.geojson` before serving the site locally.
